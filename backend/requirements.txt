fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0
pysqlite3>=0.5.0; platform_system == "Darwin"
pysqlite3-binary>=0.5.3; platform_system != "Darwin"
python-multipart>=0.0.9
# The LLM adapters speak HTTP directly (app/llm/openai_compat.py,
# anthropic_client.py) rather than using the vendor SDKs - see the adapter
# table in PLAN.md. httpx must therefore be declared here in its own right:
# it used to arrive only as a transitive dependency of the openai and
# anthropic packages, so dropping those unused SDKs without this line would
# have taken httpx with them and broken every provider call.
httpx>=0.27.0
# 0.13.1 is the newest genanki that exists; the tree shipped `>=0.13.3` from
# the initial commit, which no index can satisfy - so a clean
# `pip install -r requirements.txt` (and the Docker build) failed here.
genanki>=0.13.1
pymupdf>=1.24.0
python-pptx>=0.6.23
python-docx>=1.1.0
Pillow>=10.0
pydantic>=2.0
cryptography>=42.0
